To run the `capture_deltas.py`, the packages `digilent.adept.runtime_*.deb` and `digilent.waveforms_*.deb` have to be installed on the device.

## Analysis
Install uv, and install the dependencies and lastly create the jupyter kernel
`uv run python -m ipykernel install --user --name="must-analyze" --display-name="must-analyze Analysis kernel"`
