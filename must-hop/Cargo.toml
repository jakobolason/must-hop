[package]
name = "must-hop"
version = "0.1.1"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "A multi hop network orchestrator, implementing LoRa functionality with lora-rs"
repository = "https://github.com/jakobolason/must-hop"

[dependencies]

defmt = "1.0.1"
serde = { version = "1.0.228", default-features = false, features = ["derive"] }
postcard = { version = "1.1.3", default-features = false, features = [] }
heapless = { version = "0.9.2", features = ["serde"] }
embassy-time = { version = "0.5.0", features = [] }
embassy-sync = { version = "0.8.0" }

log = { version = "0.4.29", optional = true }

embedded-hal = { version = "1.0.0", optional = true }

lora-phy = { workspace = true }
lora-modulation = { workspace = true }


[features]
default = [
  "postcard/defmt",
  "postcard/use-defmt",
  "heapless/defmt",
  "embassy-time/defmt",
  "embassy-time/defmt-timestamp-uptime",
  "embassy-sync/defmt",
]
in_std = ["log", "embassy-time/std", "serde/std"]
debug = ["embedded-hal"]

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread", "time", "sync"] }
